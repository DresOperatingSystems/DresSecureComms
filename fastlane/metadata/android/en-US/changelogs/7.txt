Fixes the URL scanner freeze; it now checks links in a single request instead of blocking on VirusTotal. added Save to contacts, which now saves into your encrypted vault. Call log entries and conversations open a full menu (Call, Message, Save to contacts, Copy number, Delete), you can copy or save a number during a call, and long pressing a message copies it. Adds a File Scan section (engine coming soon, from Hypatia).
